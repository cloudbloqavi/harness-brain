# weather-cli — 2026-Jun-05

## 4c8d1f2 — Add --units flag (metric/imperial)

**What changed:** Added a `--units metric|imperial` flag; defaults to the
locale, falls back to metric.

**Why:** US users kept getting Celsius with no way to switch.

**Files:** `src/cli.ts`, `src/format.ts`, `tests/units.test.ts`

**Flags / to-dos:** None.
