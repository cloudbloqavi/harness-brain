# ledger-web — 2026-Jun-07

## b21e0d4 — Migrate transactions view to ledger-api /v2

**What changed:** Switched the transactions table to the new cursor-paginated
`/v2/transactions` endpoint and added infinite scroll.

**Why:** Downstream of the `ledger-api` change the same day (7f3a9c1); `/v1`
is being sunset in 90 days, so the client had to move before then.

**Files:** `src/api/transactions.ts`, `src/views/Transactions.tsx`

**Flags / to-dos:** Remove the `/v1` fallback shim once `ledger-api` completes
its sunset. Tracks the related `ledger-api` deprecation.
