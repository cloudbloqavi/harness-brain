# example-project — 2026-Jun-06

## a1b2c3d — Add rate limiting to the public API

**What changed:** Introduced a token-bucket rate limiter on all `/api/public/*`
routes, defaulting to 100 req/min per API key.

**Why:** A burst of unauthenticated traffic degraded latency for paying
customers last week; this caps blast radius.

**Files:** `src/middleware/rate-limit.ts`, `src/server.ts`, `tests/rate-limit.test.ts`

**Flags / to-dos:** Limits are hardcoded — move to `.harness`/config before GA.
